{
  "name": "multiplayer-chess-clock",
  "version": "0.1.0",
  "description": "Multiplayer chess clock webapp written with Purecsript",
  "scripts": {
    "start": "npm run bundle-css && concurrently npm:serve-*",
    "serve-js": "parcel public/index.html --no-source-maps --open",
    "serve-purs": "spago build --watch",
    "build": "npm run bundle-css && npm run bundle-purs && npm run bundle-js",
    "bundle-css": "npx tailwindcss build -o public/tailwind.css",
    "bundle-purs": "spago bundle-app --to out.js",
    "bundle-js": "parcel build public/index.html --no-source-maps",
    "prod": "rm -rf ./dist; NODE_ENV=production npm run bundle-css && npm run bundle-purs && npm run bundle-js"
  },
  "license": "MIT",
  "dependencies": {
    "autoprefixer": "^10.2.4",
    "tailwindcss": "^2.0.3"
  },
  "devDependencies": {
    "concurrently": "^6.0.0",
    "cssnano": "^4.1.10"
  }
}
